name: vuln-mcp
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - httpx
      - python-dotenv
      - pyjwt
      - cryptography
      - snowflake-connector-python
      - snowflake-core
      - requests
      - pandas
